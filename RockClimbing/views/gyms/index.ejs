<%- include("../partials/header") %>
<div class="container">
    <header class="jumbotron">
        <div class="container">
            <h1>Welcome To Rock Climbing!!!</h1>
            <p>View our hand-picked rock climbing gyms from all over the world</p>
            <p>
                <a class="btn btn-primary btn-large" href="/gyms/new">Add New GYM</a>
            </p>
        </div>
    </header>

    <!-- <div class="row text-center">
        <!-- <% gyms.forEach(function(gym){%> -->
        <!-- <div class="col-md-3 col-sm-6">
            <div class="card">               
                <img src = <%=gym.image%> class="card-img-top-index" >
                <div class= "card-footer align-items-center d-flex justify-content-center">
                    <h5 class="mb-0"><%= gym.name%></h5>
                </div>
                <a href="/gyms/<%= gym._id %>" class="btn btn-primary">More Info</a>            
            </div>
        </div>
        <!-- <% });%> -->
    <!-- </div> -->
    <div class="row text-center">
        <% for(var i = 0; i < gyms.length; i++){ %>
        <% if(i<4){%>
            <div class="col-md-3 col-sm-6">
        <%}else{%>
            <div class="col-md-3 col-sm-6 mt-4">
        <% } %>
        <!-- <div class="col-md-3 col-sm-6"> -->
            <div class="card">               
                <img src = <%=gyms[i].image%> class="card-img-top-index" >
                <div class= "card-footer align-items-center d-flex justify-content-center">
                    <h5 class="mb-0"><%= gyms[i].name%></h5>
                </div>
                <a href="/gyms/<%= gyms[i]._id %>" class="btn btn-primary">More Info</a>            
            </div>
        </div>
        <% };%>
    </div>
</div>
<%- include("../partials/footer") %>